to run: (make sure you are in the right folder "/qauditcontacts (master)")

(if terminal not opened)
- open terminal by pressing 
    ctrl + ~


(if terminal is open skip above step)
- type the following and press enter
    npm run dev